import React from "react";
import { Layout } from "@/components/layout";
const Catelogue = () => {
  return <Layout breadcrumbs={["Report and Analytics"]}>Catelogue</Layout>;
};

export default Catelogue;
